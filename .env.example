# Backend API base URL (NestJS). Trailing slash is OK.
VITE_API_BASE_URL=http://localhost:3000/

# Display only — informational. Treasury/issuer secrets must NEVER live here.
VITE_ASSET_CODE=HTGe
VITE_ENV_LABEL=development
